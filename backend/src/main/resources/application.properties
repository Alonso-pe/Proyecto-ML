spring.application.name=backend

# DB Connection (Supabase PostgreSQL)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://db.bctghlyyciumixipuels.supabase.co:5432/postgres}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:6o5tGUT1W40NGu8a}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Recomendado para Supabase (SSL)
spring.datasource.hikari.data-source-properties.sslmode=${SPRING_DATASOURCE_SSLMODE:require}

# Flyway migrations: allow enabling/disabling via env var (default=true)
spring.flyway.enabled=${SPRING_FLYWAY_ENABLED:true}

# JWT
jwt.secret=QxvQ0X1x5whP8J4c4m97Q2nuv2cGq3bA2Yp9Yf6tVnT4Jk0zBq5Lh2sRj8Wc0XeR
jwt.expiration-ms=86400000

# Migo API (read token from environment variable for safety)
# Set `MIGO_API_TOKEN` in your environment; leave empty to disable real calls in dev.
migo.api.token=ySRwqAAYJbH0oei6CGqz7qxGf9BoKszkoHFkJlFYK6bEBoESKHTYZIWln1qU
migo.api.url=https://api.migo.pe/api/v1/dni
